<div class="container">
    <h1>{{title}}</h1>
</div>
<div class="container well">
    <div class="list-group">
        {{#each teams}}
            <a href="#" class="list-group-item">{{name}}</a>  
        {{/each}}
    </div>
</div>